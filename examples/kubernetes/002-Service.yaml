apiVersion: v1
kind: Service
metadata:
  name: fabric-mc
  labels:
    app: minecraft-fabric

spec:
  type: NodePort
  ports:
    - protocol: TCP
      name: minecraft
      port: 25565
      targetPort: 25565
  externalIPs:
    - 192.168.1.100
  selector:
    app: minecraft-fabric
